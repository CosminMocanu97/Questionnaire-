To run this project you only need the following:

### `npm install`

Run this command to install all the required dependencies to run the app.

### `npm start`

Starts the app in the development mode.
